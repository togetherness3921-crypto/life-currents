// This component will be the sidebar for chat history
import React from 'react';

const ChatSidebar = () => {
  return (
    <div className="h-full w-full bg-card p-2">
      <p className="text-foreground">Chat History Sidebar</p>
    </div>
  );
};

export default ChatSidebar;
